import { expect } from "chai";

import * as studentFunctions from '../solution.js';

const {
  test
} = studentFunctions;

// TODO: implement tests
describe('test', () =>  {
    it("test", () => {
        expect(1).to.equal(1);
    });
})
